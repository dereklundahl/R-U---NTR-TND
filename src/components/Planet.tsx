import React from 'react';
import { Text } from 'react-native';

const Planet = () => {
    return (
        <div>
            <Text>{Planet.name}</Text>
        </div>
    );
}

export default Planet;
